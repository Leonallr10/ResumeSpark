import { HomeResumeLoader } from "@/components/home-resume-loader";

export const metadata = {
  title: "LaTeX Resume Editor | ResumeSpark",
  description: "CodeMirror 6 LaTeX Resume Editor with SyncTeX bidirectional PDF sync and AI tailoring.",
};

export default function LatexEditorPage() {
  return <HomeResumeLoader />;
}
